import { gameBoardModule } from './modules/UI/gameBoard.js'

describe('Should render the gameboard to the screen', () => {
  it('should render 9 cells', () => {
    gameBoardModule.renderGameBoard()

    expect(gameBoardModule.renderGameBoard());
  })
})